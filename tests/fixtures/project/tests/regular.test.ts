// a non-Playwright / non-Vitest-specific test file to verify targeting
// eslint-disable-next-line @typescript-eslint/no-unused-vars -- fixture intentionally unused
const value = 1;

export function regularHelper() {
    return value + 1;
}
